<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="AllFs">  


    <select id="AllfsView" resultType="com.green.allfs.vo.AllfsVo">
	     
	    	
			SELECT * 
			FROM ALLFS
			WHERE A_LONGITUDE =#{totloc_Lng}	
					  
	
    </select>

    
</mapper>   